services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: squadbuilder
      POSTGRES_PASSWORD: squadbuilder
      POSTGRES_DB: squadbuilder
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
